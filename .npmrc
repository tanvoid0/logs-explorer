# Project-specific npm configuration
# Use the standard npm registry
registry=https://registry.npmjs.org/

# Use project-specific cache and store to avoid global interference
cache=./.npm-cache
store-dir=./.pnpm-store

# Set log level to normal (not error) to see warnings
loglevel=warn

# Prevent workspace configuration conflicts
workspace-concurrency=1
